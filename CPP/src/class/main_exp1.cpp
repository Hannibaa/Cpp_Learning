#include <iostream>
#include "exemple_h.hpp"

// template function should be defined in header file rather than declared 


int main() {

	//auto c = functions::cubic(20.5f);

	//auto s = functions::square(20.5f);

	auto f = functions::add(3.f, 66.6f);

	std::cout << "float is " << f << end_;

	return 0;
}